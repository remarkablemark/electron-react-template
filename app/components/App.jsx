'use strict';

/**
 * Module dependencies.
 */
var React = require('react');

/**
 * App component.
 */
module.exports = React.createClass({
    render: function() {
        return (
            <h1>
                Welcome to electron-react-template!
            </h1>
        );
    }
});
